gulp_skeleton
=============
node.jsインストール  
bowerインストール  

`$ npm install`  
macだと`$ sudo npm install`

`$ bower install`

`$ gulp`

`$ gulp watch`

で行けると思うんだけど、  
必要であれば  
gulp.jsインストール  
sassインストール  
CoffeeScriptインストール

こっちはbrowser sync  
いろいろあってよくわからn
